name: Build & Deploy
on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy ActionIntro
        uses: actions/checkout@v3
        run: git pull origin
